// Password Reset Page (when password is forgotten)
// User will be redirected here when they click on the password reset link in email.

import { RecoverPasswordLayout } from "./components/layout";



export default function RecoverPassword() {
    return (
        <RecoverPasswordLayout />
    );
}
